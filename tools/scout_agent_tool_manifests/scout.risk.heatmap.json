{
  "id": "scout.risk.heatmap",
  "version": "0.1.0",
  "description": "Build a candidate-only calibrated route risk heatmap from local risk attribution diagnostics.",
  "command": {
    "argv": [
      "{python}",
      "-m",
      "scout_agent_builtin_tools",
      "risk-heatmap",
      "--input",
      "{input}",
      "--output",
      "{output}",
      "--json"
    ],
    "dry_run_argument": "--dry-run"
  },
  "mode": "workspace_write",
  "input_schema_ref": "schemas/scout_agent/risk_heatmap.request.json",
  "output_schema_ref": "schemas/scout_agent/tool_result.json",
  "allowed_reads": [
    "pretrip.route_risk",
    "pretrip.workspace.risk_diagnostic"
  ],
  "allowed_writes": [
    "pretrip.workspace.risk_heatmap"
  ],
  "forbidden_writes": [
    "phase1.runtime",
    "phase2.brain.observed_facts",
    "live.safety_api"
  ],
  "requires_authorization": {
    "kind": "user_or_operator"
  },
  "trace": {
    "required": true,
    "event_kind": "agent_tool_invocation"
  }
}
